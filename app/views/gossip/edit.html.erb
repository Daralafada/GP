<h1> Editer le gossip </h1>

<%= form_for @gossip do |i| %>
	
	<div class = "form-group">
		<label> <%= @gossip.title %></label>
		<%= f.text_field :title, class: 'form-control' %>
	</div>

	<div class = "form-group">
		<label> Contenu</label>
		<%= f.text_area :title, class: 'form-control' %>
	</div>

	<div class = "form-group">
		<button type="submmit"> Modifier </button>
	</div>

<%end%>